(ns booketariand.core-test
  (:require [clojure.test :refer :all]
            [clojure.java.shell :only [:sh]]
            [booketariand.pdf.extractor :refer :all]))


(deftest ensure_pdf-test
  (testing "booketariand.pdf.extractor/test_pdf_ns"
    (let [results (booketariand.pdf.extractor/test_pdf_ns)]
      ;;(println results)
      (is (= results "success"))
      )))
