class BillingMemberRate
  include PermanentRecord

  f_float    :rate, :default => 0.00
  f_date     :effective_on, :default => Date.now
  belongs_to :organization
  belongs_to :case
  belongs_to :member

end
