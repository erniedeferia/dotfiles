class CaseNoteTag
  include CoreModel
  f_string     :value
  f_object_id  :organization_id

  scope  :for_organization, ->(org_id) { where :organization_id => org_id }

  # Returns "tag1,tag2,..."
  def  to_string_array
     CaseNoteTag.all.each { |t| t.value }.join(",");
  end

end
