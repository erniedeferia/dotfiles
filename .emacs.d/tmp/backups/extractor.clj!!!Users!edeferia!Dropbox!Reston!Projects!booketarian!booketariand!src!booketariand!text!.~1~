;; Delegates to LibreOffice or GraphicsMagick to convert files to PDF.
;;

(ns booketariand.text.extractor
  (:use   [me.raynes.fs :as fsutil :exclude [parents name]] ;; requires JDK 7 (1.7)
          [clojure.java.shell :only [sh]]
      ))
