require 'test_helper'

class LoggedInUserTest < ActionDispatch::IntegrationTest
  test "logged in user goes to home page" do
   assert_difference 'User.count' do
    signup_user
    login_user
   end
  end
end
