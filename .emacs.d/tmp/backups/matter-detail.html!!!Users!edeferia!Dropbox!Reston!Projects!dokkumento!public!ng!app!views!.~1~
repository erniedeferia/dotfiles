 <div class='row-fluid'  ng-controller='CoffeeCtrl'>
  <div class='col-md-4'>
     <h4> This illustrates the ng-repeat tag used with an html list </h4>
     <ul>
       <li ng-repeat="thing in coffeeThings">
         {{ thing.id }} -> {{ thing.name }}
       </li>
     </ul>
     <div ng-controller="GrindingCtrl">
      <ul>
        <li ng-repeat="g in grinders">
         {{ g.id }} ->  {{ g.name }}
        </li>
      </ul>
     </div>
  </div> <!-- closes .span6 -->
  <div class='col-md-4'>
     <div ng-controller="BrewerCtrl">
       <h4>Pick one of these<h4>
       <button ng-click="automated()">Automated</button>
       <button ng-click="dripper()">Dripper</button>
       <br />
       <br />
       <h4>Or create your own</h4>
       <input ng-model="customBrewer"></input>
       <button ng-click="custom(customBrewer)">My Own</button>
       <br />
       <br />
       <p>You have chocen the <strong>{{ brewer }} </strong> type of brewer</p>
     </div>
  </div> <!-- .closes .span6 -->
 </div> <!-- closes .row-fluid -->
