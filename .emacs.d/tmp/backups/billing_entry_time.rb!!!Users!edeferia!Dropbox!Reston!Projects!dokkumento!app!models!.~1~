class BillingEntryTime < BillingEntry
  #
  # This can support billing for fractions of hours, where 0.5 = 30 minutes,
  # 0.25 = 15 minutes, and so on.
  #
  f_float  :time_units, :default => 1.0

  embeds_one :billing_member_rate

  accepts_nested_attributes_for :billing_member_rate

end
